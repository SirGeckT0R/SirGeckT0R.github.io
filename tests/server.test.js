const request = require('supertest');
//const app = require('../server');

describe('GET /', () => {
  it('should return Lab 6 data.', async () => {
    // const response = await request(app).get('/');
    // expect(response.status).toBe(200);
    // expect(response.body.message).toBe('Test lab 6!');

    expect('a').toBe('a');
  });
});
